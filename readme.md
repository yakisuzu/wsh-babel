# before to build
`npm install`  

# build
## build with gulp webpack
`npm run build-gulp`  

## watch to js file
`npm run build-gulp watch`  

# run for windows
run to js file in dest folder at cscript.exe  
